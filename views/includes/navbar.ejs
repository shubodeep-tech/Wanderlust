<nav class="navbar navbar-expand-md bg-white border-bottom sticky-top">
  <div class="container-fluid">

    
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <i class="fa-regular fa-compass text-danger fs-4 me-2"></i>
    </a>

   
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

     
    <form
  class="search-form mx-auto my-2 my-md-0"
  role="search"
  action="/search"
  method="GET"
>
  <input
    type="text"
    name="query"   
    placeholder="Search stays (e.g. Hyderabad under 8000)"
    value="<%= typeof query !== 'undefined' ? query : '' %>"
    class="search-input"
  />
  <button class="search-btn" type="submit">
    <i class="fa-solid fa-magnifying-glass"></i>
  </button>
</form>
      <!-- RIGHT -->
      <div class="navbar-nav ms-auto align-items-center">

        <a class="btn btn-danger rounded-pill px-3 me-3" href="/listings/new">
          List Your Property
        </a>

        <% if (typeof currUser !== "undefined" && currUser) { %>
          <a class="nav-link me-2" href="/listings/wishlist">
            ❤️ Wishlist
          </a>
        <% } %>

       <% if (typeof currUser !== "undefined" && currUser) { %>
        <a class="nav-link" href="/logout">Logout</a>
     <% } else { %>
        <a class="nav-link" href="/signup">Signup</a>
        <a class="nav-link" href="/login">Login</a>
     <% } %>

      </div>
    </div>
  </div>
</nav>